<div class="my-cart bg-white  rounded-[4px]  z-[2000]  backdrop-blur-lg relative md:w-[386px] sm:w-full w-full ">
  <div class=" header  select-none absolute top-0 right-0 left-0 flex md:p-1 p md:pt-[20px] pt-1 p-2  w-full z-50 justify-between items-end ">
    <h3>
      Cart (
      <span data-ajax-cart-bind="item_count" class="header__cart-quantity">
        {{ cart.item_count }}
      </span>
      )
    </h3>
    <h3 class="cart-btn md:pr-[10px]">Close</h3>
  </div>

  <div class="  overflow-y-scroll h-full pb-14   md:pt-14 pt-14  md:p-1 p-1" data-ajax-cart-section>
    <div class="my-cart__items " data-ajax-cart-section-scroll>
      {% for line_item in cart.items %}
        {% render 'cart-card' with { product: line_item , index: forloop.index } %}
      {% endfor %}
    </div>
  </div>

  <div class=" cart-footer flex gap-2 justify-center align-middle absolute left-0 right-0 bottom-2    w-full ">
    {% form 'cart', cart, id: 'my-ajax-cart-form' %}
      <button
        class=" px-20 flex w-full h-el justify-center items-center bg-black bg-opacity-80 text-white hover:bg-opacity-5 hover:text-black rounded-sm  backdrop-blur-lg "
        type="submit"
        name="checkout"
      >
        <h3>Checkout</h3>
      </button>
    {% endform %}
  </div>
</div>

{% schema %} { "name": "My Ajax cart" } {% endschema %}
