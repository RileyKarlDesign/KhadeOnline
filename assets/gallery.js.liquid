

// error of unkowen module is bs 

const swiper = new Swiper('.product-swiper', {
    // Optional parameters
  
    slidesPerView: 'auto',
    centeredSlides: false,
    roundLengths: true,
    loop: true,
    mousewheel: true,
    spaceBetween: 0, 
    slideToClickedSlide: true,

  });