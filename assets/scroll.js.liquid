// console.log(' scroll 1.0');

// -------------------


document.addEventListener('DOMContentLoaded', function () {


  // let windowWidth = window.innerWidth;
  // let postScroll = document.querySelector(".post-wrapper");
  // let scrollingHorizontally = null;
  // let expanded = false 

  // let scrollHori = (event) => {

  //   if (scrollingHorizontally) {
  //     postScroll.scrollBy({
  //       left: event.deltaY < 0 ? -70 : 70,
  //     });
  //     event.preventDefault();

  //     // check if the user has reached the end of the slider
  //     if (postScroll.scrollLeft >= postScroll.scrollWidth - postScroll.clientWidth) {
  //       scrollingHorizontally = true;
  //     }
  //   } else {
  //     // (scroll the page up/down)
  //     return true;
  //   }

  // }



  // if( windowWidth < 768 ) {
  //     scrollingHorizontally=true
  //     postScroll.addEventListener("wheel", (event) => scrollHori(event) )
  //   }else{
  //       scrollingHorizontally=false
  //       postScroll.removeEventListener("wheel", scrollHori);
  //    }



  // window.addEventListener("resize", () => {
  //   windowWidth = window.innerWidth;
  //   // console.log(windowWidth)

  //   if( windowWidth < 768 ){
  //     if(!scrollingHorizontally ){
  //       scrollingHorizontally=true
  //       postScroll.addEventListener("wheel", (event) => scrollHori(event) )
  //     }
  //     }else if(windowWidth > 768){
  //       if(scrollingHorizontally ){
  //         scrollingHorizontally=false
  //         postScroll.removeEventListener("wheel", scrollHori);
  //      }

  //     }
  // })








})